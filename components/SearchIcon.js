import React from 'react';
import {Icon} from 'react-native-elements'

const SearchIcon = () => {
    return (<Icon name="search" type="material" size={30} color="white" containerStyle={{flexDirection: 'row', marginRight: 5}} />);
}

export default SearchIcon;